import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'juan.collaguazo.camera',
  appName: 'JCameraJuanCollaguazo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
